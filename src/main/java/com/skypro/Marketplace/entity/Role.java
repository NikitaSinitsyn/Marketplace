package com.skypro.Marketplace.entity;

/**
 * Enum representing the roles of users in the marketplace application.
 * Users can have either the USER role or the ADMIN role.
 */
public enum Role {
    USER, ADMIN
}
